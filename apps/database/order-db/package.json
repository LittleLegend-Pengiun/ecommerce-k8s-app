{
  "name": "order-db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install-java": "./mvnw clean install",
    "dev": "./mvnw spring-boot:run",
    "stop-java": "./mvnw spring-boot:stop",
    "build": "./mvnw package -DskipTests",
    "image:build": "docker build -t order-db .",
    "init:db": "docker-compose up"
  }
}
